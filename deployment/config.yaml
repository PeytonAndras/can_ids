# Deployment configuration for RAMN + CARLA real-time IDS
window_ms: 100
smoothing:
  consecutive_windows: 1  # No smoothing - immediate detection
models:
  isolation_forest: ../models/unsupervised/new/isolation_forest.joblib
thresholds: ../results/unsupervised/new/thresholds.json
ensemble: isolation_forest
logging:
  path: logs/alerts.jsonl
  level: INFO
telemetry:
  http_port: 8080
