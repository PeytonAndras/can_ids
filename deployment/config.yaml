# Deployment configuration for RAMN + CARLA real-time IDS
window_ms: 100
smoothing:
  consecutive_windows: 4  # raised for additional temporal filtering
models:
  isolation_forest: ../models/unsupervised/no_traffic/isolation_forest.joblib
thresholds: ../results/unsupervised/no_traffic/thresholds.json
ensemble: isolation_forest
logging:
  path: logs/alerts.jsonl
  level: INFO
telemetry:
  http_port: 8080
