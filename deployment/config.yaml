# Deployment configuration for RAMN + CARLA real-time IDS
window_ms: 100
smoothing:
  consecutive_windows: 3
models:
  isolation_forest: ../models/unsupervised/carla_only/isolation_forest.joblib
  pca: ../models/unsupervised/carla_only/pca_reconstruction.joblib
thresholds: ../results/unsupervised/carla_only/thresholds.json
ensemble: logical_and
logging:
  path: logs/alerts.jsonl
  level: INFO
telemetry:
  http_port: 8080
